[project]
name = "terminal-libs"
version = "0.1.0"
description = "My libs for the terminal"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.13,<0.13"]
build-backend = "uv_build"
