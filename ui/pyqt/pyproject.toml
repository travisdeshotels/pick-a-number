[project]
name = "pick-a-number-pyqt-ui"
version = "1.0.0"
description = "Frontend for pick a number"
requires-python = ">=3.14"
dependencies = [
  "PyQt6",
  "requests"
]

[build-system]
requires = ["uv_build>=0.12.10,<0.13"]
build-backend = "uv_build"
